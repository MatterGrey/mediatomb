Fix (const char *) conversion to (char *).
==========================================================================
---
 src/tools.cc |    6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

Index: mediatomb-0.12.1/src/tools.cc
===================================================================
--- mediatomb-0.12.1.orig/src/tools.cc
+++ mediatomb-0.12.1/src/tools.cc
@@ -377,17 +377,17 @@
             *buf << '%' << hex[hi] << hex[lo];
         }
     }
     return buf->toString();
 }
 
 String url_unescape(String str)
 {
-    char *data = str.c_str();
+    char *data = const_cast<char *>(str.c_str());
     int len = str.length();
     Ref<StringBuffer> buf(new StringBuffer(len));
 
     int i = 0;
     while (i < len)
     {
         char c = data[i++];
         if (c == '%')
@@ -395,23 +395,23 @@
             if (i + 2 > len)
                 break; // avoid buffer overrun
             char chi = data[i++];
             char clo = data[i++];
             int hi, lo;
 
             const char *pos;
 
-            pos = strchr(hex, chi);
+            pos = strchr(const_cast<char *>(hex), chi);
             if (!pos)
                 hi = 0;
             else
                 hi = pos - hex;
 
-            pos = strchr(hex, clo);
+            pos = strchr(const_cast<char *>(hex), clo);
             if (!pos)
                 lo = 0;
             else
                 lo = pos - hex;
 
             int ascii = (hi << 4) | lo;
             *buf << (char)ascii;
         }
